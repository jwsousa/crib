extends layout

block content
  h1#header Cribbage by CS productions
  h2.message Welcome
  .game
    #otherhand.section
      h3 Other Player's Hand:
      .cards
    #numbers.section
      #count
    #flipcrib.section
      #flip
        .cards
      #crib
        .cards
    #play.section
      .cards
    #hand.section
      h3 My Hand:
      .cards

  b.message
  br
  a#newGame.button(onClick='startNewGame();') Start New Game
  a#newHand.button.hidden(onClick='startNewHand();') Start New Hand
  script
    console.log('Start');
      //- makeHandDivsMap();



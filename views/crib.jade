extends layout

block content
  h1#header Cribbage by CS productions
  h2#topmessage.message Welcome
  .game
    #otherhand.section
      textarea(rows='3')
      h3 Other Player's Hand:
      .cards
    #numbers.section
      h4#opponentscore
      h4#count
      h4#score
    #flipcrib.section
      #flip
        .cards
      #crib
        .cards
    #play.section
      .cards
    #hand.section
      h3 My Hand:
      .cards
      textarea(rows='3')

  #bottommessage.message
  br
  a#newGame.button(onClick='startNewGame();') Start New Game
  a#newHand.button.hidden(onClick='startNextHand();') Start Next Hand
  script
    console.log('Start');
      //- makeHandDivsMap();


